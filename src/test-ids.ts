/** Shadow part exposed for the fixed-positioned trigger wrapper. */
export const PART_TRIGGER = 'trigger'

/** Shadow part exposed for the fixed-positioned menu wrapper. */
export const PART_MENU = 'menu'

/** Stable id of the menu wrapper, referenced by the trigger's `aria-controls`. */
export const MENU_ID = 'flying-menu-popup'

/** Custom event fired when the open state changes (cancelable). */
export const EVENT_TOGGLE = 'flying-menu-toggle'

/** Custom event fired when the resting corner changes. */
export const EVENT_CORNER = 'flying-menu-corner'

/** Tag name of the custom element. */
export const TAG_NAME = 'flying-menu'
